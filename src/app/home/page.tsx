import VideoList from "@/app/components/VideoList";

const page = () => {
    return (
        <div className="mx-auto max-w-7xl">
            <VideoList/>
        </div>
    );
};

export default page;
