import VideoList from "@/app/components/VideoList";

const page = async () => {
    return (
        <div className="mx-auto max-w-7xl">
            <VideoList/>
        </div>
    );
};

export default page;
