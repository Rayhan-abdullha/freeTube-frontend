const Logo = ({ className }: { className?: string }) => {
    return (
        <h3 className={`text-xl font-bold ${className}`}>
            Productivize 
        </h3>
    );
};

export default Logo;
